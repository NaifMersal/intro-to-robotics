# Part 4 — How Does a TV Remote Work? {.sdaia-dark background-gradient="linear-gradient(135deg, #1C355E, #00C9A7)"}

## Invisible Control

How does a TV remote talk to the TV? It blinks a light that we can't see!

- **Infrared (IR)** is light with a frequency lower than red light.
- Our eyes can't see it, but the robot's secondary "electronic eye" can.
- It's like a flashlight sending a secret **Morse code** at high speed.

## The Hardware

To receive signals, we use an **IR Receiver Module**.

::: columns
::: {.column width="50%"}
**Connections:**

1. **GND** → GND
2. **VCC** → 5V
3. **Signal** → **Pin 3** (Digital)
:::

::: {.column width="50%"}
![IR Receiver Attachment](../../assets/ir_remote.png)
:::
:::

## Activity: IR Intuition {background-color="#00C9A7" .sdaia-dark}

**Can you see the invisible light?**

1. Take your phone camera.
2. Point the IR Remote at the camera lens.
3. Press a button.
4. **Look at the screen!** Do you see a purple/pink blink?

> **Why?** Digital cameras can "see" slightly more of the spectrum than human eyes.


# Part 5 — Decoding the Remote {.sdaia-dark background-gradient="linear-gradient(135deg, #1C355E, #00C9A7)"}

## Setup the Library

Every robot needs a "dictionary" to understand IR. We use the **IRremote** library.

1. Download the library as a ZIP file from:
   [IRremote-4.7.1.zip](https://downloads.arduino.cc/libraries/github.com/z3t0/IRremote-4.7.1.zip)
2. In the Arduino IDE, go to **Sketch > Include Library > Add .ZIP Library...**
3. Select the downloaded ZIP file.

![Add Library as ZIP](../../assets/add_lib_as_zip.png)

::: {.callout-important}
## Protocol Check
The Keyestudio remote uses the **NEC Protocol**. The library will handle this for us automatically!
:::

## Activity: Scanning Code {background-color="#00C9A7" .sdaia-dark .smaller}

Before we control the car, we need to know what "numbers" your buttons send.

1. Open a new sketch in Arduino IDE and paste this code:
```cpp
#include <IRremote.hpp>

int RECV_PIN = 3;

void setup() {
    Serial.begin(9600);
    IrReceiver.begin(RECV_PIN, ENABLE_LED_FEEDBACK); 
}

void loop() {
    if (IrReceiver.decode()) {
        if (!(IrReceiver.decodedIRData.flags & IRDATA_FLAGS_IS_REPEAT)) {
            Serial.print("Command: 0x");
            Serial.println(IrReceiver.decodedIRData.command, HEX);
        }
        IrReceiver.resume(); 
    }
}
```

## Activity: Mapping the Remote {background-color="#00C9A7" .sdaia-dark}

2. Upload the code and open the **Serial Monitor** (9600 baud).
3. Press buttons and **write down the HEX codes** for:
   - Up Arrow (▲)
   - Down Arrow (▼)
   - Left Arrow (◄)
   - Right Arrow (►)
   - OK / Stop

## What are those codes?

When you press a button, the remote sends a command number like `0x46`.

- This is a **HEXADECIMAL** number (Base-16).
- It's like a unique ID for that specific button.
- If you use a different remote, the numbers will be different!
