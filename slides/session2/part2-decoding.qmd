# Part 2 — Decoding the Remote {.sdaia-dark background-gradient="linear-gradient(135deg, #1C355E, #00C9A7)"}

## Setup the Library

Every robot needs a "dictionary" to understand IR. We use the **IRremote** library.

1. Go to **Tools > Manage Libraries...**
2. Search for: **IRremote**
3. Install the version by **Armin Joachimsmeyer** (v3.0 or higher).

::: {.callout-important}
## Protocol Check
The Keyestudio remote uses the **NEC Protocol**. The library will handle this for us automatically!
:::

## Activity: Scanning for Codes {background-color="#00C9A7" .sdaia-dark}

Before we control the car, we need to know what "numbers" your buttons send.

1. Open Arduino IDE.
2. Go to **File > Examples > IRremote > SimpleReceiver**.
3. Change `IR_RECEIVE_PIN` to **2**.
4. Open the **Serial Monitor** (115200 baud).
5. Press buttons and **write down the HEX codes** for:
   - Up Arrow (▲)
   - Down Arrow (▼)
   - Left Arrow (◄)
   - Right Arrow (►)
   - OK / Stop

## What are those codes?

When you press a button, the remote sends a number like `0xFF18E7`.

- This is a **HEXADECIMAL** number (Base-16).
- It's like a unique ID for that specific button.
- If you use a different remote, the numbers will be different!
